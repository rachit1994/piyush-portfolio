export type WorkVideoItem = {
  slug: string;
  videoId: string;
  title: string;
  categorySlug: string;
};

export type WorkImageItem = {
  slug: string;
  src: string;
  alt: string;
  categorySlug: string;
};
